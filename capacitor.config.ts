import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Protected_Prueba_003',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
